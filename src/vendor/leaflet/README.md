Vendored Leaflet 1.9.4 (BSD-2-Clause) from the npm "leaflet" package.
We use divIcon markers, so the default marker PNGs are intentionally not copied.
To update: bump the leaflet devDependency and re-copy dist/leaflet.js + leaflet.css.
