node_modules/
dist/
*.zip
.DS_Store
__pycache__/

# never commit HAR captures (may contain session data)
*.har

# local-only real captures for verification (never committed — public repo)
test/.live/

# backend database files (sqlite + WAL sidecars)
server/data/
